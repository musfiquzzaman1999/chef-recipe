import React from 'react';
import Header from './Header';

const Home = () => {
    const [chefs,setChefs]=([]);
    return (
        <div>
            <Header></Header>

        </div>
    );
};

export default Home;